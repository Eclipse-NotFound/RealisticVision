package
{
   import flash.display.Sprite;
   /** Public callbacks carried across sibling loader domains by native display tree. */
   public class RVExplorationCarrier extends Sprite
   {
      public var api:Object;
   }
}
