# v0.29.0 适中柔化部署回执

2026-09-20 用户明确“部署”后，已将源码提交 `dadd417` 的已验构建部署到 `release/RealisticVisionMod.swf`。适中柔化4/q3，20,608字节，SHA256 `5C26D604BA6AE33A92A5D11F8A6F6ABCFBF8BDE9663BAF3AB68E919A32B8C242`。

为保证与完整回归的产物逐字节一致，没有重编译或改动版本字符串；运行时标记仍为 `v0.29.0-candidate`，该标记现已部署。

## 发布门禁

| 检查 | 结果 |
|---|---|
| 构建、版本与类清单 | 复用成功构建，源码/测试SWF与归档指纹一致；仅RealisticVisionMod，无fe.*存根 |
| 核心自动验证 | 复用同指纹16项专项、1000射线、墙形39项、小墙10方向、接缝8项、完整训练房和运动回放 |
| 授权与宿主 | 用户本轮明确部署；application.xml指向根pfe.swf，本体不修改，宿主指纹与已验基线一致 |
| 备份 | 新建v0.28.2备份并核验SHA，同名文件存在则中止；此前备份保留 |
| 替换 | .NET原子替换，正式产物与测试SWF指纹一致 |
| 独立启动 | 正式路径新版标记、设置注册成功、17条心跳至2881 |
| 回滚、记忆、版本记录 | 下方明确回滚；MEMORY/journal/本回执和对照页状态更新，无独立changelog |
| 提交 | 部署记录与状态提交到模组仓库，SWF/备份遵循既有忽略规则 |

## 启动证据与范围

独立应用ID `rv-deploy-smoke-v0290-20260920-110715`，PID51952；隐藏窗口，实际游戏目录为应用根，直接加载正式pfe.swf及release。日志与存储位于该测试应用专用目录，不使用真实pfe存档。

日志确认 `init ok v0.29.0-candidate enabled=true`、`msw settings registered`，心跳1至2881共17条。这证明正式构建加载、设置页注册调用成功和主循环持续运行；本轮未操作F12、用户存档或战斗。核心阴影行为采用同一构建已完成的回归证据，不重复声称为此次启动检查的结果。

已核对进程命令行归属于本测试描述符后关闭PID51952，并删除自有临时描述符。普通沙箱读取进程身份被拒绝，经工具审核完成身份核对与清理。用户游戏实例未操作，**保存并正常退出、重新启动游戏后加载新版**。

已有67ms单次性能峰值、长期战斗未验、classic粗格掠角和F12配置写入问题仍按[候选报告](2026-09-20-current-soft-candidate.md)记录，本轮不扩修。

## 回滚与未变文件

- 回滚文件：`build/release_backup_v0282_before_v0290_20260920-110643.swf`。
- 备份SHA256：`A8A7DAE9A755ACD7FC93101003AACE2C8CF2A0FF6A507318B3B7A7CAADB685E2`。
- 回滚方法：将该备份复制到 `release/RealisticVisionMod.swf`，核对上述SHA，然后正常重启游戏。
- 配置保留current/dim=.35/debug=0，SHA `5136EE1923A77F5F10EAB1B3994B64CBD67B09D687A7E829FB1E81D2E79A3C7F`。
- 根pfe.swf未改，SHA `5300EC4874E0404D298BB58C5D2A7469E82F93B455AD29AD64DD2E29D17241E7`。
- 源码未改，SHA `34F62748734B000D26D438668A455A5C4D2CC59B9FEF37CD78D078AA854BD10B`。

原始记录：[部署回执](deployment-v0290/deployment-receipt.json)、[独立实例](deployment-v0290/smoke-session.json)、[启动日志](deployment-v0290/vision-startup.txt)。对照页仅更新部署状态与链接，原始图像、测量、源码和部署前release指纹保留；页面变更前后SHA单独记录。
