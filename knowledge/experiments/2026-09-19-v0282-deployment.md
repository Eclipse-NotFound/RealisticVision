被测技能：remains-release-gate、remains-swf-patching、remains-auto-testing、remains-runtime-debug、remains-mod-memory（未声明版本）；test-report 2026-09-16.1
技能归属：Remains工作区 .agents/skills；test-report为C:/Users/hello/.agents/skills
测试工作区：D:/Program Files/Steam/steamapps/common/Remains/mods/RealisticVision

# v0.28.2 正式部署回执（2026-09-19）

用户明确“请部署”后，已将源码提交37c25ba的已验候选部署到release/RealisticVisionMod.swf。18,877字节，SHA256 `A8A7DAE9A755ACD7FC93101003AACE2C8CF2A0FF6A507318B3B7A7CAADB685E2`。包含classic接缝修复与既有a583f1a念力守卫。

## 门禁记录

| 检查 | 结果 |
|---|---|
| 构建与版本 | 复用上一轮成功构建，不重新编译；候选/源码指纹与已归档证据一致；版本v0.28.2，FFDec仅模组类 |
| 核心自动验证 | 复用同一源码的39项墙形、8项接缝、10方向与完整训练房同输入结果，详见修复报告 |
| 授权 | 本任务用户明确“请部署” |
| 宿主与loader | application.xml指向根pfe.swf；根SWF不修改，实际启动6个loader全init returned |
| 备份 | 新备份v0.28.0-grabfix.1成功，旧备份均保留 |
| 替换 | 核验备份后原子替换；正式SWF与已验候选指纹一致 |
| 启动与入口 | 独立正式路径实例出现新版本、设置注册、22条心跳至3781；3次实际F12进入cycleMode |
| 回滚 | 见下方明确文件与步骤 |
| 记忆/版本记录 | MEMORY及journal更新；无单独changelog，源码37c25ba/D24与本部署记录承接 |
| 提交 | 本报告、证据及状态一起提交；构建/备份按既有规则不入git |

原子替换初次调用File.Replace时，PowerShell将普通null备份参数传为空路径而失败。停在该步骤并先记journal；核对正式仍是旧版、备份完好后，以`[NullString]::Value`传入明确的.NET null重新执行成功。没有覆盖唯一备份或带失败产物上线。

## 实际启动证据与边界

应用ID `rv-deploy-smoke-v0282-20260919-232730`，PID24556。使用独立描述符、实际游戏根作为应用目录，直接加载正式pfe.swf与现有release；存储自然隔离，不使用真实pfe存档。窗口隐藏，无前台切换。确认进程命令行与窗口归属后，仅向它发送3次F12。

- `init ok v0.28.2 enabled=true`、`msw settings registered`，心跳1…3781。
- SandyMod、RConnectMod、RVision、MSWMod、TDFC、RandomRoomsMod均init returned。
- 三次模式入口复现既有`SecurityError: fileWriteResource`，调用栈为onKeyDown→cycleMode→saveConfig；异常被捕获，之后心跳继续。此为既有设置持久化问题，未作为本轮新渲染故障。正式配置原样保留，重启仍默认current。
- 自有进程已终止，临时描述符已移除。未操作用户正在游玩的进程，需用户保存并正常重启以加载新SWF。

这次检查覆盖正常startup、现有六模组加载和主菜单模式入口；墙影核心行为沿用同指纹候选的实景/夹具证据。没有把它说成原存档逐帧复现、三模式UI画面验收、六模组共同战斗或长期性能测试。强制重绘约增加6ms等修复报告中的边界仍成立。

证据目录：[deployment-v0282](deployment-v0282/)（deployment-receipt.json、smoke-session.json、vision-startup.txt、loader-startup.txt）。修复报告：[2026-09-19-classic-wall-seam-fix.md](2026-09-19-classic-wall-seam-fix.md)。对照页只更新发布状态，部署前图像与输入/候选指纹不改。

## 保留文件与回滚

- 当前配置：current / dim=.35 / debug=0，SHA256 `5136EE1923A77F5F10EAB1B3994B64CBD67B09D687A7E829FB1E81D2E79A3C7F`。
- 根pfe.swf未改，SHA256 `5300EC4874E0404D298BB58C5D2A7469E82F93B455AD29AD64DD2E29D17241E7`。
- 本次回滚文件：`build/release_backup_v0280_grabfix1_before_v0282_20260919.swf`，SHA256 `E4E5ED82D5E65201A57A712F7BEBC6FDB499E7822C131B414A67700107401969`。
- 回滚操作：将该备份复制到`release/RealisticVisionMod.swf`，校验SHA，再正常重启游戏。旧v0.28.0、v0.27.1回滚点及MSW WIP stash均保留。

技能实战采用固定门禁、已有授权复用、不可覆盖备份、独立实例、定向自有窗口输入与错误边界记录。本轮没有修改技能；报告登记全局台账，供后续维护核对。
